pub mod machine {
    use crate::Error;
    pub fn run(opcode: &[u8]) -> Result<(), Error> {
        todo!("The aarch64 backend is not yet implemented");
    }
}

pub mod asm {
    use crate::Error;
    pub fn run(opcode: &[u8]) -> Result<(), Error> {
        todo!("The aarch64 backend is not yet implemented");
    }
}
